export default abstract class ThirdPartyTicket {
  getReference(): string {
    return "";
  }

  updateNumbers(count: number): void {
    void count;
  }

  closeTicket(count: number): void {
    void count;
  }
}
